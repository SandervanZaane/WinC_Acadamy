// function calculateSupply(age, amount) {
//     let calculateSupply = (100 - age) * 365 * amount;

//     console.log("You will need " + calculateSupply + " to last you until the ripe old age of 100")
// };

// calculateSupply('99', '1');
// calculateSupply('0', '1');
// calculateSupply('90', '1');


/////////////////////

let movie = { title: "joker", duration: "90", stars: ['testitle', 'john', 'bert'] };

function movie(title, duration, stars[]) {
    console.log(movie.title + " lasts for" + movie.duration + "minutes." + movie.stars);
};

movie(sander, 30, "ik ben de ster");

////// van de laatste oplossing snap ik weinig ////////